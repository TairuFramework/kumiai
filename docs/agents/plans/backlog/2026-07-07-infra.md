# Infra debt — tooling, manifests, hooks, docs

**Priority:** backlog — mechanical. Land opportunistically alongside work that already touches the
file, not as its own PR.
**Origin:** 2026-07-02 audit (commit `bb343d9`), archived milestone
`archive/2026-07-audit-remediation.md`.
**Merged 2026-07-28** from `next/2026-07-07-infra-batch.md` and `backlog/2026-07-07-infra-cleanup.md`.
The two were split by which PR would carry them; that PR never happened, and both halves have the
same priority and the same "take it while you are already in the file" character.

## Retired findings

Kept so they are not re-filed.

- ~~**`package.json:10` — lint cannot fail in CI.**~~ — **void, verified 2026-07-23.** The finding
  assumed CI runs `pnpm run lint`. It does not: `.github/workflows/build-test.yml` delegates to
  kigu's shared workflow, which runs `pnpm exec biome ci .`
  (`../kigu/.github/workflows/build-test.yml:54`) — non-writing, and it fails on violations. The root
  `lint` script is still `biome check --write ./packages ./tests`, but that is developer convenience,
  not a CI hole. (Local note: an `rtk` shim intercepts `pnpm run lint`; use `rtk proxy pnpm run lint`
  for real output.)
- ~~**No release workflow.**~~ — **decided against 2026-07-23.** Releases stay manual
  (`pnpm release` → `changeset publish`). No stack repo has a publish workflow, kigu included, so
  automating it is a stack-wide change rather than a kumiai one. Revisit only if the stack decides
  to automate releases everywhere.

## Package manifests

- **vitest undeclared in 4 of 10 packages** — `mls`, `hub-client`, `hub-server`, `hub-tunnel` run
  `vitest run` without declaring it; works only via `nodeLinker: hoisted`. Fix: add
  `"vitest": "catalog:"` to each. (Corrected 2026-07-23: the audit said "5 of 7" and named
  `hub-protocol`, which now declares it; the repo has since grown to ten packages.)
- **No `LICENSE` file** at root or in any package despite `"license": "MIT"` in all 10 manifests —
  npm tarballs ship no license text. Fix: root LICENSE, included per package. (Re-verified
  2026-07-23: still no `LICENSE` anywhere in the tree.)
- All packages — `"exports": { ".": "./lib/index.js" }` has no `types` condition and no
  `"./package.json"` export. Fix:
  `{ ".": { "types": "./lib/index.d.ts", "default": "./lib/index.js" }, "./package.json": "./package.json" }`.
- `packages/hub-tunnel/package.json:47` — `test:types` omits `--skipLibCheck` unlike the other six.
  Align.
- `packages/{hub-protocol,hub-client,hub-server}` — no `README.md` (blank npm pages).
- `tests/e2e-expo/package.json:2-3` — name `e2e-expo` / version `1.0.0` vs the
  `@kumiai/integration-tests` / `0.1.0` convention. Rename.
- tsconfig drift — `hub-protocol`/`hub-client` main tsconfigs lack `"lib": ["es2025","dom"]` (only in
  `tsconfig.test.json`); `hub-server/tsconfig.json` uniquely bakes `"types": ["node"]`. Align on one
  shape.
- `package.json:7` — `packageManager` pin has no integrity hash. Add the `+sha512.…` suffix.
- `package.json:10` — lint covers only `./packages ./tests`; root-level files never linted. Run
  biome on `.`.
- `pnpm-workspace.yaml:38` — `minimumReleaseAgeExclude` configured but `minimumReleaseAge` unset —
  dead config. Set it or delete the block.
- `pnpm-workspace.yaml` catalog — `react` pinned exact (`19.2.3`) and `ts-mls` on an RC while
  everything else is `^`; the RC upgrade is tracked in `backlog/ts-mls-upstream.md`. Worth a comment.

## Turbo task graph

Mostly **done** on `feat/mls-permission-enforcement` (commit `62c524e`), after the stale-hit bug
below actually fired: a warm cache replayed a passing `test:types` for `e2e-expo` across the branch's
whole MLS API reshape, so a broken consumer reached an open PR reporting green.

- ~~`turbo.json:8-9` — `test:types`/`test:unit` lack `dependsOn: ["^build:types"]` and
  dependency-aware inputs.~~ **Done.** `build:types` is now a turbo task and both test tasks depend
  on `^build:types`, so an upstream source change invalidates every downstream typecheck and a cold
  cache bootstraps the build.
- ~~`turbo.json:7` — `build:js` outputs `lib/**` capture `.d.ts` emitted by the non-turbo
  `build:types`.~~ **Done.** The two builds now claim disjoint outputs (`lib/**/*.js` and
  `lib/**/*.d.ts`).
- **Still open** — `build:js` depended on `^clean`, but no package defines a `clean` script (all
  define `build:clean`), so cleaning never happened and stale files persist in `lib/`. The dead
  `^clean` was dropped rather than wired up, because root `build` runs `build:types` before
  `build:js`: a clean hung off `build:js` would delete the declarations `build:types` just emitted.
  Wiring cleaning in needs the build restructured — e.g. a single `build:clean` both builds depend
  on, with root `build` becoming one `turbo run build:types build:js`.

## Hooks

- `.githooks/pre-commit:6` — `biome check --write --staged` fixes files but never re-stages them, so
  the unfixed version is committed. Fix: `git add -u` after the fix step, or run non-writing and
  fail.
- `.githooks/pre-commit:13` — runs `build:types` (mutating `lib/`) instead of a `--noEmit` check. Use
  the `test:types` scripts.

## CI (kigu-side changes)

- kigu `build-test.yml` — CI caches only the pnpm store; no turbo cache persistence, so full
  build+test reruns on both Node 24 and 26 every run. Fix: cache `.turbo` or wire a remote cache.
- kigu `build-test.yml` — the TS-readiness step is `continue-on-error: true`; `--stableTypeOrdering`
  regressions are invisible unless someone opens the log. Surface as a job summary/annotation.
- `.github/workflows/e2e-android.yml`, `e2e-ios.yml` — full emulator/simulator E2E on every push/PR
  with no path filters, for an app exercising only `@kumiai/mls`. Fix: gate on `paths:`
  (`packages/mls`, `tests/e2e-expo`) or a label.

## Docs and conventions

- `AGENTS.md` — references `../kigu/docs/repo-split-design.md`, which does not exist. Fix: point to
  `kigu/docs/stack.md` or restore the file.
- `docs/index.md` — omits `agents/development.md` from the doc map.
- `packages/rpc/src/hub-mux.ts:174,176` — `readonly bus` / `readonly mailbox` use the prohibited
  `readonly` keyword. Fix: drop the modifiers. (Lines and the second field name corrected
  2026-07-23.) Note `packages/rpc/src/cursor.ts:23,26` also read `readonly`, but as index signatures
  inside branded-type declarations — the idiomatic branding pattern, not the prohibited property
  modifier. Leave those alone.

## Scope

`turbo.json`, `.githooks/`, package manifests, tsconfigs, root `LICENSE`, `AGENTS.md`,
`docs/index.md`, one `@kumiai/rpc` one-liner. Three findings are kigu-side, not kumiai.
