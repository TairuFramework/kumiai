{
  "name": "@kumiai/hub-conformance",
  "version": "0.4.1",
  "description": "Conformance test suite for the @kumiai/hub-protocol HubStore contract",
  "keywords": [
    "hub",
    "conformance",
    "testing"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/TairuFramework/kumiai",
    "directory": "packages/hub-conformance"
  },
  "license": "MIT",
  "sideEffects": false,
  "type": "module",
  "exports": {
    ".": "./lib/index.js"
  },
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/*"
  ],
  "scripts": {
    "build": "pnpm run build:clean && pnpm run build:js && pnpm run build:types",
    "build:clean": "del lib",
    "build:js": "swc src -d ./lib --config-file ../../node_modules/@kigu/dev/swc.json --strip-leading-paths",
    "build:types": "tsc --emitDeclarationOnly --skipLibCheck",
    "prepublishOnly": "pnpm run build",
    "test": "pnpm run test:types",
    "test:types": "tsc --noEmit --skipLibCheck -p tsconfig.test.json"
  },
  "dependencies": {
    "@kumiai/hub-protocol": "workspace:^"
  },
  "devDependencies": {
    "vitest": "catalog:"
  },
  "peerDependencies": {
    "vitest": "catalog:"
  }
}
