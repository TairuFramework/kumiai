---
"@kumiai/hub-client": minor
---

Add the log-class publish and fetch surface (additive): optional `retain`, `expectedHead`, and `publishID` publish fields, a `SubscribeOptions` retention field, and the `FetchTopicParams`/`FetchTopicResult` types.
